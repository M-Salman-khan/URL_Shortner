<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication - Log In</title>
    <link rel="shortcut icon" href="../assests/AUTH.png" type="image/x-icon">
    <style>
        :root{
            color-scheme: dark;
        }
        *{
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        body{
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100dvh;
            width: 100dvw;
        }
        .loginPage{
            border: 2px solid white;
            width: 400px;
            padding: 30px;
        }
        .loginPage h1{
            margin-bottom: 40px;
            width: fit-content;
            margin-top: 10px;
            border-bottom: 4px solid white;
        }
        form{
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100%;
            position: relative;
        }
        .inputFields{
            display: flex;
            flex-direction: column;
        }
        input{
            padding: 7px 2px;
            border: none;
            border-bottom: 2px solid white;
            background-color: transparent;
            outline: none;
        }
        label{
            margin-top: 20px;
            margin-bottom: 10px;
        }
        form button{
            margin-top: 40px;
            padding: 10px 30px;
            background-color: white;
            color: black;
            border: 2px solid white;
        }
        form button:hover{
            background-color: transparent;
            color: white;
            cursor: pointer;
            transition-duration: 0.4s;
        }
    </style>
</head>
<body>

        
        <div class="loginPage">
        <h1>Log In</h1>
        <form action="/user/login" method="post">
            <div class="inputFields">
                <label for="email">Enter your email id : </label>
                <input type="email" name="email">
                <label for="password">Enter your password : </label>
                <input type="password" name="password">
                <% if (locals.error) { %>
                    <div><p style="color: red;"><%= locals.error %></p></div>
                <% } %>
            </div>
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>